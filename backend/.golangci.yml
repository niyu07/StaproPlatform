version: "2"

linters:
  enable:
    - errcheck
    - revive
    - govet
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
