version: "2"

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - revive
    - govet
    - ineffassign
    - staticcheck
    - unused

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
